title: Busco Trabajo CABA
description: Sitio Web con ofertas de trabajo en CABA. > # this means to ignore newlines until "baseurl:"


baseurl: "/blog" # the subpath of your site, e.g. /blog

url: "http://www.buscotrabajocaba.com" # the base hostname & protocol for your site, e.g. http://example.com


permalink: ':title/'
# Tracker
analytics: # Google Analytics

# Build Settings
markdown: kramdown
plugins:
  - jekyll-sitemap
  - jekyll-paginate
  - jemoji

paginate: 8
paginate_path: "/page/:num"

exclude: ["node_modules", "gulpfile.js", "package.json", "yarn.lock"]

sass:
  style: compressed
  

